<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipeApp.Models.StartPage"
             xmlns:viewmodel="clr-namespace:RecipeApp.ViewModels"
             x:DataType="viewmodel:StartPageViewModel">
    <VerticalStackLayout>
        <Label 
            Text="{Binding Date}"
            HorizontalOptions="Center"
            FontSize="20" >
        </Label>
        <Label
            Text="{Binding RecipeTitle}">
        </Label>
        
        <!--Det ska stå dagens datum följt av "Frukost, Lunch, Middag och ev Mellanmål. Du ska kunna trycka på en knapp för att generera ett random recept
        och en knapp för att göra en egen entry med titeln på maten. Om det finns ett recept ska du kunna trycka på antingen måltids-titeln eller namnet, för att se hela receptet
        då söker den på recept-id:t och hämtar hela receptet
        Recept-id ska sparas i en databas tillsammans med datumet så att det kan hämtas igen när man startar om appen-->
       
        

        <CollectionView
            ItemsSource="{Binding CategoryNames}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label
                       Text="{Binding .}"
                        FontSize="20">
                    </Label>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>-->

    </VerticalStackLayout>
</ContentPage>